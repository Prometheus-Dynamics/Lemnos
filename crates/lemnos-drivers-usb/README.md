# lemnos-drivers-usb

`lemnos-drivers-usb` provides the built-in generic USB driver for Lemnos.

## Scope

This crate exports:

- `UsbDriver`
- a companion `manifest()` helper

It supports the bundled generic-driver story for USB interfaces in tests and applications.
