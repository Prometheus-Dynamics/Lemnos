# lemnos-drivers-spi

`lemnos-drivers-spi` provides the built-in generic SPI driver for Lemnos.

## Scope

This crate exports:

- `SpiDriver`
- a companion `manifest()` helper

Use it through the facade crate's `builtin-drivers` feature or directly in lower-level runtime setups.
