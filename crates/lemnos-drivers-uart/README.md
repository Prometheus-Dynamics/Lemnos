# lemnos-drivers-uart

`lemnos-drivers-uart` provides the built-in generic UART driver for Lemnos.

## Scope

This crate exports:

- `UartDriver`
- a companion `manifest()` helper

It is most often used through `lemnos` with `builtin-drivers` enabled.
