# lemnos-drivers-pwm

`lemnos-drivers-pwm` provides the built-in generic PWM driver for Lemnos.

## Scope

This crate exports:

- `PwmDriver`
- a companion `manifest()` helper

It is intended for the bundled built-in driver path and for runtime tests that need generic PWM behavior.
